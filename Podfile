# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BottomNavBar' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BottomNavBar
     pod 'SwiftyJSON', '~> 4.1.0'
    pod 'Alamofire', '~> 4.7.3'
    pod 'Kingfisher', '~> 5.0'
  target 'BottomNavBarTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BottomNavBarUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
